\chapter{Optimization methods}\label{chap:optimization-methods}

% IDEA of the chapter: This is the second, more technical, background chapter that overviews common optimization methods (as in software tools, frameworks, etc.) used in high-performance computing; especially those that are relevant to our work (data layout and traversal optimizations, parallelization, autotuning, etc.; but we might include some other methods as well for completeness). It should explain the state of the art in this area and provide context for our contributions. It roughly corresponds to the "Related work" section of a typical research paper.

As discussed in \cref{chap:code-optimization}, code optimization is crucial for achieving efficient execution of programs on modern hardware architectures such as multi-core CPUs and GPUs. In this chapter, we outline several prominent optimization methods and frameworks that have been developed to facilitate (automated) code optimization in high-performance computing applications, putting particular emphasis on those that are relevant to our work targeting data layout and traversal optimizations.

Code optimization methods can target different points in the compilation and execution pipeline. Some methods operate above the source code level, providing high-level abstractions or domain-specific languages (DSLs) that allow developers to express computations in a way that is amenable to more powerful optimizations. Other methods perform optimizations at the source code level, possibly using annotations or pragmas to guide the optimization process.

More low-level methods operate on intermediate representations (IRs) or directly on the generated machine code, applying transformations to improve performance based on static analysis or empirical measurements. Some methods may also involve runtime systems that adaptively optimize code during execution based on observed performance characteristics; in such cases, methods like just-in-time (JIT) compilation techniques may be employed to generate optimized code on-the-fly based on current execution patterns.

We primarily focus on abstractions and frameworks that operate above or at the source code level, as these are most relevant to our work. However, we also briefly discuss the wider landscape of optimization methods to provide context for our contributions.


\section{Compiler optimizations}\label{sec:compiler-optimizations}

Compiler optimizations are a fundamental aspect of modern compilers. In high-performance computing, compilers such as LLVM~\cite{lattner2004llvm} and GCC~\cite{gcc} implement a wide range of optimization techniques to improve the performance of source code, starting from basic optimizations that remove abstractions and redundant computations, inline functions, perform sub-expression elimination, etc. More advanced optimizations include loop transformations (e.g., loop unrolling, loop fusion, invariant code motion), vectorization, and inter-procedural optimizations that analyze and optimize across function boundaries.

In the context of typical high-performance (scientific computing) applications with quite simple control flow and heavy numerical computations, these optimizations mainly allow using higher-level abstractions (e.g., \cpp templates, mdspan, \ldots) without significant performance penalties (most notably, when using so-called zero-cost abstractions~\cite{stroustrup2012foundations}). They can also optimize arithmetic expressions, eliminate redundant computations, and perform some basic loop transformations. However, these optimizations by themselves are often insufficient to achieve optimal performance on modern hardware architectures, especially for complex loop nests and data access patterns.

Some compilers and frameworks, such as MLIR~\cite{lattner2021mlir}, provide extensible intermediate representations that allow custom representation and optimization passes to be implemented --- some targeting specific domains or hardware architectures. This is particularly useful for domain-specific languages (DSLs) that can leverage MLIR to implement their own optimization pipelines, including polyhedral optimizations, autotuning, and other transformations not typically part of general-purpose compilers. While MLIR is built on top of traditional SSA-based IRs, it allows defining custom dialects and facilitates boilerplate such as parsing, code generation, and so on.


\subsection{Polyhedral optimization}\label{sec:polyhedral-optimization}

Workloads comprising nested loops with affine bounds and affine memory accesses (Static Control Parts, or SCoPs) are common in high-performance computing applications (see, e.g., \citeauthor{bacon1994compiler}~\cite{bacon1994compiler}). To target such applications, \citeauthor{feautrier1992some}~\cite{feautrier1992some} and others pioneered the use of the polyhedral model to represent and optimize computations in loop nests. Polyhedral optimization frameworks, such as \textbf{Polly}~\cite{grosser2012polly} (in LLVM), \textbf{GRAPHITE}~\cite{trifunovic2010graphite} (in GCC), and \textbf{PLuTo}~\cite{bondhugula2008pluto} (source-to-source), use the powerful mathematical framework of the polyhedral model (provided by libraries such as ISL~\cite{verdoolaege2010isl}) to represent computations in loops in a more compact and analyzable form compared to traditional compiler IRs such as Static Single Assignment (SSA) forms. Polyhedra offer a way to represent data layouts, loop iteration spaces, and dependencies between computations in a unified manner.

Polyhedral optimization is the application of integer linear algebra to optimize loop nests with affine bounds and affine memory accesses. The main idea is to represent the iteration space of loops, as well as memory accesses and dependencies, using polyhedra. This representation allows, for example, finding hyperplanes orthogonal to data dependencies that can represent parallelizable loops, or finding transformations of the iteration space that improve data locality or enable vectorization.

There are works that apply the theory of polyhedra and integer linear programming to optimize data layouts for improved cache performance, such as the work by \citeauthor{clauss2000automatic}~\cite{clauss2000automatic} that uses Ehrhart polynomials to change accesses to arrays into consecutive memory accesses, effectively optimizing the data layout for the target cache hierarchy.

Many works iterate on the basic polyhedral model to extend its applicability or improve the optimization results by combining it with cost models, heuristics, or machine learning techniques (e.g., \citeauthor{baghdadi2021deep}~\cite{baghdadi2021deep}). Some works show benefits of combining polyhedral optimization based on cost models or heuristics with autotuning~\cite{chelini2020automatic} or human insights~\cite{zinenko2014clint,bagneres2016opening}.

Many DSL compilers (e.g., Halide~\cite{ragan2013halide}, Tiramisu~\cite{baghdadi2019tiramisu}) that will be discussed in \cref{sec:abstractions} use polyhedral optimization techniques (or derived versions) as part of their optimization pipelines. These DSLs often provide higher-level abstractions that allow developers to guide the optimization process using directives that can be mapped to polyhedral transformations.

\textbf{Polygeist}~\cite{moses2021polygeist} is a tool that translates \cpp code to MLIR\@. It uses the Clang frontend to parse C/\cpp source code and then raises the resulting low-level LLVM IR to higher-level MLIR representations, particularly the affine dialect. This allows Polygeist to apply MLIR's polyhedral optimization passes (e.g., affine fusion, loop tiling, and parallelization) to originally imperative C/\cpp code. It combines these polyhedral optimizations with traditional SSA-based compiler optimizations, outperforming both Polly and PLuTo on the PolyBench~\cite{pouchet2012polybench} benchmark suite.

Since polyhedral optimizations are powerful tools that can fundamentally restructure the code, some works have focused on improving the interpretability of the transformations and their results, such as the work by \citeauthor{bagneres2016opening}~\cite{bagneres2016opening} that translates the scheduling decisions of polyhedral optimizers into a human-readable set of transformation directives --- e.g., loop interchange, strip-mine (tiling), \ldots --- that describe the transformations in a more intuitive way.


\section{Autotuning}\label{sec:autotuning}

Autotuning techniques are powerful methods for optimizing performance of applications based on empirical performance measurements or cost models. They involve performance-cost model-based and empirical search methods for exploring large parameter spaces (\emph{search space}s) to find optimal configurations. They can control various parameters, such as algorithmic choices, data layouts, tiling sizes, unrolling factors, and parallelization strategies, or even tune build configurations and compiler flags.

Many search algorithms have been proposed for exploring the search space, performing local or global optimization. Simple methods such as grid search and random search can be effective for search spaces with a small number of parameters. Hill climbing methods greedily explore the search space by iteratively moving towards better configurations based on performance measurements. These methods are surprisingly effective in many cases, especially when the search space is not too large or when the performance landscape does not have many local minima. Even methods that simply combine random search with local search (e.g., by performing random restarts) can be quite effective in practice.

More sophisticated methods include simulated annealing, genetic algorithms, swarm optimization, Bayesian optimization, and many others. Many of these methods balance exploration and exploitation in different ways, and their effectiveness can depend on the specific characteristics of the search space and the performance landscape.

\textbf{OpenTuner}~\cite{ansel2014opentuner} is the first framework that combines multiple search techniques (e.g., genetic algorithms, simulated annealing, etc.) in ensembles (based on multi-armed bandits) with a flexible API for defining the search space and the objective function. It is a general-purpose autotuning framework in Python that allows users to define their own search spaces and objective functions and perform black-box evaluations of the target application with different configurations from the search space.

\textbf{ATF}~\cite{rasch2018atf} is a C++ framework that builds on top of OpenTuner and a domain-specific autotuner CLTune. It provides options for pruning the search space by defining constraints on the parameters (e.g., divisibility constraints for tiling sizes) as well as employing parallelism when generating the search space.

Tools such as \textbf{Hyperopt}~\cite{bergstra2015hyperopt} and \textbf{Optuna}~\cite{akiba2019optuna} are general-purpose optimization frameworks (originally built for optimizing hyperparameters of machine learning models) that use Bayesian optimization techniques to efficiently explore large search spaces with a limited number of evaluations. Bayesian optimization involves building a surrogate model of the performance landscape based on observed measurements, and using this model to guide the search towards promising regions of the search space.

These tools can be used for autotuning by defining the search space and objective function in a way suitable for optimizing code performance. Optuna stands out for its define-by-run API, which allows for dynamic construction of the search space during the optimization process. This can be particularly useful for complex optimization problems where the search space may not be known a priori, cannot be efficiently represented in a static manner, or is too large to be exhaustively explored.

% There are many autotuning frameworks that are specific to certain domains, such as AutoTVM~\cite{chen2018tvm} for deep learning workloads, Kernel Tuner~\cite{van2019kernel} for GPU kernels

Autotuning techniques can be used in combination with other optimization methods, such as polyhedral optimization or DSLs\@. They are particularly useful for optimizing performance in cases where the performance landscape is complex and difficult to model analytically.


\section{Abstractions}\label{sec:abstractions}

Flexible abstractions allowing manipulation of data layouts and iteration orders have been the subject of extensive development. They provide a means to optimize performance for different hardware architectures without needing to change the core algorithmic logic. These abstractions can also serve as flexible API layers for libraries (such as linear algebra libraries~\cite{whaley1998automatically}).

We can broadly categorize these abstractions into the following groups:

\begin{description}
  \item[Domain-specific languages (DSLs):]\label{des:dsl} These are specialized programming languages designed to express computations in specific domains, such as image processing (Halide~\cite{ragan2013halide}), polyhedral code generation (Tiramisu~\cite{baghdadi2019tiramisu}), or array programming (Futhark~\cite{henriksen2017futhark}). They rely on sophisticated compilers or runtime systems to generate optimized code for the target hardware. These DSLs often sacrifice some generality for better analysis and optimization opportunities --- for example, Halide has a fixed computation model based on pipelines of processing stages that has limited expressivity for nested control flow or data-dependent computations.
  \item[Annotation-based tools:]\label{des:annotation-based-tools} These tools, relying on compiler extensions or external preprocessors, allow developers to annotate their code with optimization directives. Examples include OpenMP for parallelization and pragmas for loop transformations. Compared to DSLs, these tools operate on top of the target language (e.g., C/\cpp) and maintain higher generality, or at least compatibility with the host language. However, they may also be limited by specific semantics of the host language.
  \item[Parallelization frameworks and libraries:]\label{des:parallelization-frameworks} These frameworks provide abstractions for performance-portable parallel execution, multidimensional data management, and memory management across heterogeneous hardware architectures. Unlike DSLs, they operate within the host language (\cpp) and do not require custom compilers. They typically provide flexible data layout abstractions and parallel execution policies that can target different backends (e.g., multi-core CPUs, GPUs) from a single source code base. Examples include Kokkos~\cite{trott2021kokkos}, RAJA~\cite{beckingsale2019raja}, Thrust~\cite{bell2011thrust}, and Intel TBB~\cite{pheatt2008intel}.
\end{description}

These abstractions play a crucial role in optimizing applications for modern high-performance computing, as the scale and complexity of diverse hardware architectures continue to grow and traditional low-level optimization techniques alone are not sufficient to achieve optimal performance. The abstractions enable developers to apply optimizations based on domain knowledge, empirical performance data, or autotuning techniques.

Compared to hand-tuned low-level code, maintainability and portability of high-level abstractions is significantly better and the development time is reduced. Many of these abstractions also support advanced validation of the correctness of code transformations.


\subsection{Domain-specific languages (DSLs)}\label{sec:dsls}

\textbf{Halide}~\cite{ragan2013halide} is a \cpp-embedded DSL with a custom compiler and runtime system that primarily targets image and array processing applications (including regular stencil computations). It is based on defining pipelines of processing stages, where each stage represents a computation that can be optimized independently. The resulting computation pipeline is a directed acyclic graph (DAG) of stages that are subject to various transformations, such as tiling, fusion, and parallelization. These transformations can be guided by the developer using a scheduling language or by autotuning techniques.

The Halide compiler performs various checks to ensure that each transformation preserves the semantics of the original computation. It uses interval analysis, which is a less expressive but more powerful (for the target domain) representation than the polyhedral model. Halide's design allows for a high degree of optimization at the cost of expressivity: it is not suitable for more complex computations, such as those that contain imperfectly nested loops, data-dependent control flow, or non-rectangular iteration spaces.

\begin{lstlisting}[language=C++, morekeywords={Param, ImageParam, Var, RVar, Func, Float}]
#include <Halide.h>
using namespace Halide;

ImageParam A(Float(__3__2), __2, "A"), B(Float(__3__2), __2, "B"),
           C(Float(__3__2), __2, "C");
Param<float> alpha("alpha"), beta("beta");

Func gemm("gemm");
Var i("i"), j("j");
RVar k("k", __0, A.dim(__1).extent());

gemm(i, j) = beta * C(i, j);
gemm(i, j) += alpha * A(i, k) * B(k, j);

// Schedule
gemm.tile(i, j, i0, j0, i1, j1, __6__4, __6__4)
    .reorder(i1, j1, i0, j0)
    .parallel(i0)
    .vectorize(i1);
\end{lstlisting}


The above example shows how to express the matrix multiplication (GEMM) kernel in Halide. \lstinline{Param} and \lstinline{ImageParam} are used to define the input parameters and input data, respectively. The computation is defined in the \lstinline{Func} object using \lstinline{Var} (or \lstinline{RVar} for reduction variables) objects to represent dimensions involved in the computation. This computation (possibly appearing in a larger pipeline) is then subject to various transformations that can be expressed using the scheduling language. The \lstinline{gemm} computation then is, in the scheduling language, tiled with tile sizes of $64 \times 64$, the loops are then reordered to improve data locality, the outer loop is parallelized, and the inner loop is vectorized.

\textbf{Tiramisu}~\cite{baghdadi2019tiramisu} is a \cpp-embedded DSL superficially similar to Halide that targets a wider range of applications, including those with more complex control flow and data access patterns. Unlike Halide, Tiramisu is more closely based on the polyhedral model, which allows it to represent a broader class of computations, including those with imperfectly nested loops or skewed and non-rectangular iteration spaces. Its primary domains include image processing, deep learning, and general scientific computing applications, and the framework also supports sparse computations. Similarly to Halide, Tiramisu provides its own scheduling language and a compiler that performs various checks to ensure that the transformations preserve the semantics of the original computation. It can target various hardware architectures, including multi-core CPUs and GPUs. Unlike Halide, Tiramisu also supports distributed computing over multiple nodes using MPI\@.

The matrix multiplication example in Tiramisu is shown below (the syntax is simplified for illustration).

% TODO: check whether the syntax is correct
\begin{lstlisting}[language=C++, morekeywords={p_float32, var, input, computation}]
#include <tiramisu/tiramisu.h>
using namespace tiramisu;

tiramisu::init("gemm");
var i("i", __0, M), j("j", __0, N), k("k", __0, K);

input A("A", {i, k}, p_float32), B("B", {k, j}, p_float32),
      C("C", {i, j}, p_float32);
input alpha("alpha", {}, p_float32), beta("beta", {}, p_float32);

computation init("init", {i, j}, beta(__0) * C(i, j));
computation gemm("gemm", {i, j, k}, p_float32);
gemm.set_expression(init(i, j) + alpha(__0) * A(i, k) * B(k, j));

// Schedule
var i0("i0"), j0("j0"), i1("i1"), j1("j1");
gemm.tile(i, j, __6__4, __6__4, i0, j0, i1, j1);
gemm.interchange(i1, j1);
gemm.parallelize(i0);
gemm.vectorize(i1);
\end{lstlisting}

\textbf{Exo}~\cite{ikarashi2022exocompilation} is Python-embedded DSL that generates C code for specialized hardware. The target-specific instructions and optimization transformations performed on them are all defined in extensible user-level libraries. Similarly to Halide and Tiramisu, the transformations are guided by the developer using a scheduling language. Exo then performs various checks based on effect analysis to ensure program equivalence and memory safety throughout the transformations.

In Exo, the \lstinline{@instr} decorator is used to define target-specific instructions as Python functions that specify the semantics of the instruction in terms of its inputs and outputs. The \lstinline{@proc} decorator is used to define procedures that can be called from the generated code and can be subject to various transformations. The transformations in Exo are defined as Python functions that take the procedures or instructions as input and produce transformed versions of them, while ensuring that the transformations preserve the semantics of the original code. If a function transformation relies on a certain invariant (e.g., that a certain dimension of an array is divisible by a certain value), the user can specify this invariant using python assertions.

The matrix multiplication example in Exo is shown below with the same transformations as in the Halide example (the scheduling syntax is simplified for illustration).

% TODO: check whether the syntax is correct
\begin{lstlisting}[language=Python]
from exo import *
from exo.stdlib.scheduling import *

@proc
def gemm(M: size, N: size, K: size,
         alpha: f32, A: f32[M, K], B: f32[K, N],
         beta: f32, C: f32[M, N]):
  assert M % __6__4 == __0 and N % __6__4 == __0
  for i in seq(__0, M):
    for j in seq(__0, N):
      C[i, j] *= beta
      for k in seq(__0, K):
        C[i, j] += alpha * A[i, k] * B[k, j]

# Schedule
gemm = divide_loop(gemm, 'i', __6__4, ['io', 'ii'], perfect=True)
gemm = divide_loop(gemm, 'j #0', __6__4, ['jo', 'ji'], perfect=True)
gemm = reorder_loops(gemm, 'ii jo')
\end{lstlisting}

The framework then generates C code for the target hardware that can be integrated into a larger application. The generated code does not rely on any runtime system.


\subsection{Annotation-based tools}\label{sec:annotation-based-tools}

\textbf{OpenMP}~\cite{openmp_6.0_spec} is a widely used API for parallel programming in C, C++, and Fortran\@. It provides a set of compiler directives, library routines, and environment variables that allow developers to specify parallelization and synchronization of code regions, as well as to control data sharing or privatization between threads. It supports various loop transformations such as loop unrolling, tiling, and vectorization via \lstinline{#pragma omp} directives, with varying levels of support across different compilers. GPUs can be targeted using the \lstinline{#pragma omp target} directive along the \lstinline{teams} construct to control warps/wavefronts and threads within them. The advantage of OpenMP is that it is widely supported by a wide range of compilers and hardware architectures, and its implementations follow a well-defined shared specification. The specification is continually evolving to support new features and optimizations.

The main limitation of OpenMP is that it is primarily focused on parallelization and does not provide a rich set of data layout directives; nevertheless, the matrix multiplication example can be expressed in OpenMP as follows:

\begin{lstlisting}[language=C++]
void gemm(int M, int N, int K,
          float alpha, float A[M][K], float B[K][N],
          float beta, float C[M][N]) {
  #pragma omp parallel for collapse(__2) schedule(static)
  for (int i = __0; i < M; i++) {
    for (int j = __0; j < N; j++) {
      C[i][j] *= beta;
      for (int k = __0; k < K; k++) {
        C[i][j] += alpha * A[i][k] * B[k][j];
      }
    }
  }
}
\end{lstlisting}

\lstinline{collapse(__2)} is used to indicate that the two outer loops should be collapsed into a single loop for parallelization, which allows for better load balancing and performance on multi-core CPUs. However, this code does not include any other transformations such as tiling or vectorization, which would require additional directives that are not widely supported or manual code modifications.

For tiling, OpenMP provides the \lstinline{#pragma omp tile} directive that can be used to specify tiling transformations for loops, and the vectorization can be achieved using the \lstinline{#pragma omp simd} directive. The matrix multiplication step of the above code can be tiled and vectorized as follows:

\begin{lstlisting}[language=C++]
#pragma omp tile sizes(__6__4, __6__4)
#pragma omp parallel for collapse(__2) schedule(static)
for (int i = __0; i < M; i++) {
  for (int j = __0; j < N; j++) {
    float temp = __0__.__0__f;

    #pragma omp simd reduction(+:temp)
    for (int k = __0; k < K; k++) {
      temp += alpha * A[i][k] * B[k][j];
    }

    C[i][j] = beta * C[i][j] + temp;
  }
}
\end{lstlisting}

Some efforts have been made to extend OpenMP with directives for more complex, composable transformations. One example is the work by \citeauthor{kruse2018user}~\cite{kruse2018user} that extends the OpenMP API in the Clang compiler with directives for loop transformations such as tiling, reversal (the loop is executed in reverse order), interchange, and array packing (some subset of the accessed array is copied to a temporary buffer).

\textbf{Loopy}~\cite{namjoshi2016loopy} is an annotation-based tool that allows developers to express affine transformations of loop nests by specifying mappings of the schedules of the statements in nested loops as shown in the following example:

% TODO: unify the transformations here and in the earlier examples
\begin{lstlisting}
for (i = __0; i < M; i++) {
  for (j = __0; j < N; j++) {
    Init: C[i][j] = C[i][j] * beta;

    for (k = __0; k < K; k++) {
      Mult: C[i][j] += alpha * A[i][k] * B[k][j];
    }
  }
}

realign(Init, Mult, __0)
affine(Mult, {[i, j, k] -> [i, k, j]})
affine(Mult, {[i, j, k] -> [i1, j1, k1, i2, j2, k2]:
  i1 = [i/__6__4] and j1 = [j/__6__4] and k1 = [k/__6__4] and
  i2 = [i%__6__4] and j2 = [j%__6__4] and k2 = [k%__6__4]})
\end{lstlisting}

This performs loop interchange and tiling transformations on the original loop nest (the two \lstinline{affine} directives); the \lstinline{realign} directive is used to specify that the two statements should be treated as two separate loop nests (with data dependencies between them preserved), as the third argument ($0$ in this case) specifies the number of loops that should be shared between the two statements after the transformation. This allows the subsequent directives to be applied to each statement independently. The framework performs correctness checks to ensure that the transformations preserve the semantics of the original computation. One limitation of Loopy is that it does not have any support for parallelization or vectorization.


\subsection{Native tools and libraries}\label{sec:native-tools}

\textbf{CuTe} from the Cutlass~\cite{thakkar2023cutlass} framework is a collection of \cpp template abstractions that can hierarchically represent multidimensional layouts of threads and data. They are specifically designed for representing and optimizing computations on NVIDIA GPUs\@. The multidimensional layouts can represent a wide range of dense linear algebra computations, and they also allow for functional composition of layouts like in Noarr\@, which is particularly useful for common transformations such as tiling or partitioning~\cite{nvidia-cute}.

CuTe stands out for the expressiveness of its layout abstractions, including the algebraic composition of layouts. The most basic primitives are dynamic \lstinline|int{N}| and static \lstinline|Int<N>{}| integer values, possibly in tuples (\lstinline|make_tuple(int{M}, int{N})|) that can be constructed recursively --- all those collectively are called \emph{IntTuple}s.

A CuTe layout is defined as a tuple $(\text{Shape}, \text{Stride})$ (written as \lstinline{shape:stride}) where both \text{Shape} and \text{Stride} are IntTuples. Since IntTuples can be hierarchical, $(2, (2, 2)) : (1, (2, 4))$ is a valid layout that represents a two-dimensional layout of shape $2 \times 4$ where the second dimension can be indexed as a tuple of two dimensions of size $2 \times 2$ --- this describes a tiled layout where the second dimension of size $4$ is tiled into two tiles of size $2$. Each layout (or equivalently, each multidimensional dimension) can be indexed either using a tuple of appropriate shape, or using a linear index that is upper-bounded by the product of the dimensions of the shape.

CuTe defines layout compatibility (partial order between layouts): a layout $A$ is compatible with a layout $B$ if their shapes have the same size and if indices of $A$ are valid indices of $B$. This effectively means that a linear dimension of size $N$ in $A$ can be replaced by a tuple with the same total size $N$ in $B$, producing a compatible layout. For example, the layout with shape $(2, 6)$ is compatible with the layout with shape $(2, (2, 3))$ (second dimension of size $6$ has been broken into two dimensions of size $2$ and $3$).

The compatibility relation is the basis for defining layout algebra that is based on functional composition of layouts: $R = A \circ B$ is defined as $R(x) = A(B(x))$ where $B$ is applied first and then $A$ is applied to the result (this is well-defined thanks to the ability to index layouts using linear indices). $B$ is compatible with the resulting layout $R$. The abstraction then uses the functional composition of layouts to define transformations (but only showcases tiling).

This functional composition of layouts follows the same principle as Noarr. Most notably, that the right-hand-side input of the composition can be thought of as a partial function that transforms the input index space into a new index space.

\textbf{GridTools}~\cite{afanasyev2021gridtools} is a \cpp-embedded DSL that targets stencil computations on structured grids, primarily in the context of weather and climate modeling. The stencil computations are represented as a composition of abstract stages implemented as \cpp functors. The framework supports backends for various hardware architectures, including many-core CPU systems and GPUs\@. Since the stencil stages are represented in a high-level manner, the framework can apply various hardware-specific optimizations depending on the target backend.

\textbf{MDSpans}~\cite{trott2022mdspan} are a \cpp abstraction for representing multidimensional arrays with flexible data layouts. The mapping of multidimensional indices to linear memory addresses and the element access policy that translates the resulting linear index to element references are both customizable, allowing for a wide range of data layouts and special memory types. The predefined layouts include row-major, column-major, and generic strided layouts, but the abstraction also allows defining custom memory layouts. However, it does not provide any built-in mechanism for hierarchical composition of layouts or their analysis beyond recognizing the predefined ones and retrieving their strides and extents.

MDSpans are sufficient for representing simple data layouts, and their user-definable policies extend applicability beyond that. However, in such cases, they do not provide any benefits other than a uniform interface for accessing elements, which is not much different from using raw pointers and manual index calculations. In particular, they do not introduce type safety or correctness guarantees, and their interface relies on the user to correctly implement the access policies and communicate the intended semantics of the layout dimensions by other means when not using the small set of predefined layouts. This is usually sufficient when the optimized computation performs transformations such as tiling or transposition, which are supported by the \lstinline{submdspan} constructor and the predefined \lstinline{layout_transpose} policy adaptor.

\textbf{Kokkos}~\cite{trott2021kokkos} is a \cpp library that provides abstractions for parallel execution and data management across different hardware architectures; its primary focus is providing a performance-portable programming model for multi-core CPUs and GPUs\@. It provides the \lstinline{Kokkos::View} abstraction for representing multidimensional arrays with flexible data layouts and memory spaces (e.g., host memory, device memory, \ldots). The expressiveness of \lstinline{Kokkos::View} for representing data layouts is similar to that of MDSpans\@, with the same predefined layouts and user-definable mapping policies --- and the two abstractions can be easily inter-converted.

However, Kokkos views also allow specifying the memory space where the data is allocated, and they provide automatic data movement between memory spaces when necessary. Kokkos views also support shared ownership semantics similar to \cpp shared pointers. Kokkos provides a set of parallel algorithms (e.g., \lstinline{parallel_for}, \lstinline{parallel_reduce}, \ldots) that can run user-defined computations in parallel on the target backend.

To support multidimensional parallelism, Kokkos provides the \lstinline{MDRangePolicy} abstraction, which allows for defining parallel execution policies over multidimensional index spaces. The \lstinline{MDRangePolicy} parallelizes the execution of a user-defined computation in tiles of configurable sizes.

For more flexible parallelization strategies, Kokkos also provides the \lstinline{TeamPolicy}, which allows defining teams of threads that can cooperate on executing a user-defined computation with synchronization and shared memory capabilities (team scratch memory). The \lstinline{ThreadVectorRange} can be used to express vector-level parallelism within a team (or warp/wavefront on GPU\@). These policies are composable, allowing for expressing multilevel parallelism. Furthermore, the computation can be queued to a specific execution stream on the target backend to overlap computation and data movement.

\textbf{RAJA}~\cite{beckingsale2019raja} is another \cpp library that provides abstractions for platform portability in high-performance computing applications. It abstracts data layouts via the \lstinline{RAJA::Layout} class, which represents a dense multidimensional array layout, defaulting to row-major order but allowing for arbitrary permutations of the dimensions. The library also provides the \lstinline{RAJA::OffsetLayout} class, which represents layouts with arbitrary integer offsets for each dimension --- this simplifies, for example, defining stencil computations that expect padding around the data.

Both of these layout abstractions can be used to define a \lstinline{RAJA::View}, a multidimensional array abstraction that provides an interface for accessing elements based on the defined layout. The expressiveness of these abstractions is lower than that of MDSpans or Kokkos views, but they allow for the inverse mapping of linear indices to multidimensional indices.

While RAJA provides much weaker abstractions for data layouts, it provides more flexible abstractions for parallel execution compared to Kokkos\@. In particular, it allows representing complex loop nests via the \lstinline{RAJA::kernel} abstraction that can be applied to a kernel execution policy defined as a composition of compile-time constructs representing statements via template metaprogramming.

The two main building blocks for defining kernel execution policies are \lstinline{RAJA::For} and \lstinline{RAJA::Lambda} constructs, which represent a single loop level and a computation to be executed at that loop level, respectively. RAJA provides a wide range of statement building blocks that can represent tiling, running nested statements in a CUDA or HIP kernel, or performing reductions, among others. These building blocks can be given an execution policy that defines how the represented loop level is parallelized (e.g., sequentially, via OpenMP, mapping to a CUDA block or grid dimension, \ldots).

The resulting kernel execution policy represents a complex loop nest parallelized according to the specified execution policies, which is then used by the \lstinline{RAJA::kernel} abstraction to execute user-defined computations with the supplied parameters as shown in the following example:

% https://raja.readthedocs.io/en/develop/sphinx/user_guide/tutorial/kernel_nested_loop_reorder.html
\begin{lstlisting}[morekeywords={RAJA}]
RAJA::TypedRangeSegment<KIDX> KRange(kmin, kmax);
RAJA::TypedRangeSegment<JIDX> JRange(jmin, jmax);
RAJA::TypedRangeSegment<IIDX> IRange(imin, imax);

RAJA::OffsetLayout<__3> layout = RAJA::make_permuted_offset_layout
  ({{imin, imax}, {jmin, jmax}, {kmin, kmax}}, {__2, __0, __1});
RAJA::View<double, RAJA::OffsetLayout<__3>> view(data, layout);

using KJI_EXECPOL =
  RAJA::KernelPolicy<
    RAJA::statement::For<__2, RAJA::seq_exec,     // 2: k
      RAJA::statement::For<__1, RAJA::seq_exec,   // 1: j
        RAJA::statement::For<__0, RAJA::seq_exec, // 0: i
          RAJA::statement::Lambda<__0>
  > > > >;

RAJA::kernel<KJI_EXECPOL>(
  RAJA::make_tuple(/*0*/ IRange, /*1*/ JRange, /*2*/ KRange),
  [=] (/*0*/ IIDX i, /*1*/ JIDX j, /*2*/ KIDX k) {
    view(i, j, k) = i + j + k;
  });
\end{lstlisting}

In the above example, we define a 3D loop nest with iteration spaces defined by the \lstinline{KRange}, \lstinline{JRange}, and \lstinline{IRange} segments that operates on a 3D array represented by the \lstinline{RAJA::View} abstraction with a custom layout defined by the \lstinline{RAJA::OffsetLayout} class with a specific permutation of the dimensions. The view is a multidimensional array that can be accessed using the indices defined by $($\lstinline{IRange}$\times$\lstinline{JRange}$\times$\lstinline{KRange}$)$; $k$ is the majormost dimension, followed by $i$ and $j$ as the minor dimensions.


\section{Machine learning and LLMs for optimization}\label{sec:ml-and-llms-for-optimization}

% TURNING POINT: the year 2020 (introduction of GPT-3)

Recently, machine learning (ML) techniques, including large language models (LLMs), have been increasingly applied to various aspects of code optimization. The main motivation for using these techniques is their flexibility and ability to learn complex patterns from data, which can overcome limitations of traditional optimization methods that rely on hand-crafted heuristics or analytical models.

We have already discussed autotuning techniques in \cref{sec:autotuning} that can be used to tune various compiler flags and options based on empirical measurements, so here we focus on ML-based methods that are not better categorized as autotuning. What characterizes these methods is that they make decisions based on learned patterns, applying them to new code or new optimization problems without relying on empirical measurements of the target code, which would be the case for autotuning techniques (such as Bayesian optimization methods).

Some older works have applied ML techniques as surrogate models for performance prediction, such as the work by \citeauthor{fursin2011milepost}~\cite{fursin2011milepost}. Recent additions in the area of HPC include works such as \citeauthor{baghdadi2021deep}~\cite{baghdadi2021deep} that use deep learning methods as cost models in a polyhedral optimization framework (the Tiramisu compiler) or \citeauthor{adams2019learning}~\cite{adams2019learning} that use a learned cost model to guide the search in the Halide autotuning framework.

More recently, LLMs have emerged as a powerful tool for code optimization, with the ability to learn from large codebases and generate optimized code based on natural language descriptions or code snippets. Various works have explored the use of off-the-shelf LLMs or fine-tuned (or even trained from scratch) LLMs for generating directives used to optimize code, such as OpenMP directives for parallelization, scheduling directives for frameworks such as Halide or Tiramisu, or even generating optimized code directly.

Works that use LLMs for generating optimization directives include PragFormer~\cite{harel2025pragformer} that trains a transformer-based model to identify \lstinline{for} loops in C code that can be parallelized and even identify the need for privatization or reduction variables. Works such as OMPify~\cite{kadosh2023advising}, Graph2Par~\cite{chen2023learning}, and AutoParLLM~\cite{mahmud2023autoparllm} then use GNN-based models to generate OpenMP directives for parallelization with even higher accuracy.

HPC-Coder~\cite{nichols2024hpc} shows that fine-tuning LLMs on HPC code can enable them to generate OpenMP directives for parallelization with high accuracy as well as generate parallel code or MPI-based distributed code directly. The MPI code generation is further studied by works such as MPI-RICAL~\cite{schneider2023mpi} and MPI{rigen}~\cite{schneider2024mpirigen}.

Works that use LLMs or other deep learning methods for generating scheduling directives include ComPilot~\cite{merouani2025agentic}, which uses an LLM agent as a search driver when exploring the search space of transformation directives, and LOOPer~\cite{merouani2025looper}, which uses an LSTM-based solution inspired by the work of \citeauthor{baghdadi2021deep}. Both solutions achieve autoscheduling in the Tiramisu compiler.

Using ML techniques as surrogate models or requesting them to propose optimization directives has the benefit of enabling formal verification of the optimized code, as the model acts simply as a search driver in a solution that would otherwise work as an autotuning solution, trying semi-random configurations and adjusting heuristics for selecting each next configuration based on observed performance measurements.

Direct generation of optimized code goes further and allows the model to make more fundamental changes to the code --- some solutions even use the machine learning model to generate the entire optimized code directly from a natural language description (or from another high-level representation of the expected behavior of the code)~\cite{godoy2024large}.

Since LLMs are generally trained on large codebases, and only a handful of the code falls into the HPC domain, the performance of off-the-shelf LLMs on generating optimized HPC code has started to become competitive only recently, perhaps with the introduction of Codex~\cite{chen2021evaluating}. This challenge was earlier addressed by fine-tuning LLMs on HPC code~\cite{nichols2024performance} or by using smaller models trained from scratch on HPC code~\cite{xu2022systematic}. Such work has to be accompanied by standardized benchmarks and datasets of HPC code, such as PolyBench~\cite{pouchet2012polybench}, ParEval~\cite{nichols2024can}, KernelBench~\cite{ouyang2025kernelbench}, HPCorpus~\cite{kadosh2023quantifying}, or HeCBench~\cite{jin2023benchmark}.

Since the proficiency of LLMs as of the beginning of 2026 is still not sufficient for generating optimized code without supervision or verification, most works rely on using LLMs for translating between a more interpretable language and a programming model such as CUDA~\cite{palkowski2024gpt,dearing2024lassi}. One of these works, LASSI, defines a self-correcting pipeline where the LLM proposes a solution similarly to an autotuning solution, with the added benefit of being able to respond to feedback from the compiler or the verifier. This direction is used in various state-of-the-art solutions such as ComPilot~\cite{merouani2025agentic}.


\section{Summary}\label{sec:optimization-methods-summary}

\Cref{tab:optimization-methods-summary} provides an overview of the optimization methods and tools discussed in this chapter, comparing them along several key dimensions: the level of abstraction at which they operate, the optimization techniques they support, the target hardware architectures, and the degree of automation they provide.

\begin{table}[ht]
  \centering
  \scriptsize
  % \setlength{\tabcolsep}{1.75em} % Adjust column separation
  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Tool/Method} & \textbf{Targets} & \textbf{Key Features} \\
    \midrule
    \multicolumn{3}{c}{\textit{Polyhedral optimization tools}} \\
    \makecell[l]{PLuTo, Polly, GRAPHITE} & CPU & Automatic loop transformations \\
    Polygeist & CPU, GPU & C/\cpp to MLIR polyhedral opt. \\
    \midrule
    \multicolumn{3}{c}{\textit{Autotuning frameworks}} \\
    OpenTuner & Any & Ensemble search \\
    ATF & Any & Ensemble, conditional parameters \\
    Hyperopt & Any & Bayesian optimization \\
    Optuna & Any & Dynamic search spaces, pruning \\
    \midrule
    \multicolumn{3}{c}{\textit{Domain-specific languages}} \\
    Halide & CPU, GPU & Image/array pipelines, scheduling \\
    Tiramisu & CPU, GPU, MPI & Polyhedral model, scheduling \\
    Exo & Customizable & User-defined instructions \\
    \midrule
    \multicolumn{3}{c}{\textit{Annotation-based tools}} \\
    OpenMP & CPU, GPU & Parallelization directives \\
    Loopy & CPU & Affine schedule annotations \\
    \midrule
    \multicolumn{3}{c}{\textit{Library-based native tools}} \\
    MDSpan (std) & Any & Flexible layout policies \\
    CuTe (DSL) & NVIDIA GPU & Hierarchical layout algebra \\
    GridTools (DSL) & CPU, GPU & Stencil computations \\
    Kokkos, RAJA & CPU, GPU & Views, parallel execution \\
    % RAJA & CPU, GPU & Kernel policies, views \\
    \midrule
    \multicolumn{3}{c}{\textit{ML and LLM-based methods}} \\
    \makecell[l]{OMPify, Graph2Par, AutoParLLM} & CPU & GNN-based OpenMP directive generation \\
    HPC-Coder, PragFormer & CPU, GPU & Transformer-based directive generation \\
    MPI-RICAL, MPI{rigen} & CPU & MPI code generation \\
    ComPilot, LOOPer & CPU, GPU & Autoscheduling (Tiramisu) \\
    \makecell[l]{Codex, \xxx{\citeauthor{godoy2024large}}, \\ \xxx{\citeauthor{nichols2024performance}}, \xxx{\citeauthor{xu2022systematic}}} & CPU, GPU & LLM-based code generation \\
    LASSI, \xxx{\citeauthor{palkowski2024gpt}} & CPU, GPU & LLM-based code translation with feedback \\
    \midrule
    \multicolumn{3}{c}{\textit{Our contributions}} \\
    Noarr & CPU, GPU, MPI & Layout-agnostic abstractions \\
    Cellato & CPU & Stencil-specific layout agnosticism \\
    \xxx{Tutoring} & GPU & LLM-based code generation with guidance \\
    \xxx{Shifting} & CPU & LLM-based code translation and optimization \\
    \bottomrule
  \end{tabular}
  \caption{Overview of optimization methods and tools discussed in this chapter.}%
  \label{tab:optimization-methods-summary}
\end{table}
